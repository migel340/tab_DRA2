info:
  name: personel lookup
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/personels/lookup?role=STAFF"
  params:
    - name: role
      value: STAFF
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
