info:
  name: List device types
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/{{path}}?limit=20&clientId=2"
  params:
    - name: limit
      value: "20"
      type: query
    - name: clientId
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
