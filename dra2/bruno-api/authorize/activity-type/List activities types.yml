info:
  name: List activities types
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/{{path}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
