info:
  name: list clients
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/{{path}}?q="
  params:
    - name: q
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
