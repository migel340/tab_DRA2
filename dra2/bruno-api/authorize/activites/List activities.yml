info:
  name: List activities
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/{{path}}?requestId=5&page=2"
  params:
    - name: requestId
      value: "5"
      type: query
    - name: page
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
