info:
  name: List activities
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/{{path}}?requestId=13"
  params:
    - name: requestId
      value: "13"
      type: query
    - name: page
      value: "2"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
