info:
  name: create activity
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/{{path}}"
  body:
    type: json
    data: |-
      {
          "requestId": "5",
          "description": "fdssafsfa",
          "actTypeId": "4",
          "status": "REGISTERED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
