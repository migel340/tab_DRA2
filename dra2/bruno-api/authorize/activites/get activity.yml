info:
  name: get activity
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/{{path}}/26"
  params:
    - name: page
      value: "2"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
