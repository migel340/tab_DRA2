info:
  name: patch activity
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/{{path}}/26"
  body:
    type: json
    data: |-
      {
          "result": "fdssafsfa",
          "status": "REGISTERED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
